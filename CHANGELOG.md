# 版本更新日志

本项目遵循“版本号 + 日期 + 变更分类”的记录方式。每次功能、修复、性能或构建配置变动都应补充到本文件。

## [1.0.2] - 2026-08-18

### 性能优化

- 将日志读取从主线程迁移到单线程后台执行器，页面先完成布局，再异步展示内容；
- 移除首次进入页面时 `onCreate` 与 `onResume` 的重复刷新；
- 使用 `RandomAccessFile` 直接定位文件尾部，不再从头逐行扫描整个日志；
- 页面仅渲染最新 220 KB，解决抓包日志累计较多时进入页面需要 1–2 秒的问题；
- 真机测试抓包日志页面显示耗时约 523 ms。

### 新增功能

- 在运行日志和抓包日志页面增加“导出”按钮；
- 通过 Android Storage Access Framework 选择导出位置；
- 完整日志以带时间戳的 TXT 文件导出；
- 导出在后台线程执行，并显示成功或失败结果。

### 界面调整

- 日志页面改为独立标题栏和三按钮操作栏；
- 导出、刷新、清空分别使用紫色、青色、橙色高对比配色；
- 修复横向空间不足导致“抓包日志”标题换行的问题。

### 稳定性

- 日志文件上限设置为 8 MB，达到上限后自动轮转；
- 异步刷新使用 generation 标识，防止旧任务覆盖新内容；
- Activity 销毁时终止执行器并忽略过期 UI 更新。

## [1.0.1] - 2026-08-18

### 新增功能

- 完成菜鸟智脑 LSPosed 模块主体；
- 新增全部运行日志、抓包日志和设置三个主页入口；
- 设置页新增“基础”模块及“菜鸟抓包”开关；
- Hook `MtopBusiness.startRequest` 与 `CNMtopBusiness.startRequest`；
- Hook 底层 `Request$a.cch`；
- Hook `MtopResponse.setHeaderFields` 和 `setBytedata`；
- 新增请求去重机制，降低短时间重复日志；
- 新增自定义菜鸟智脑图标和大胆配色界面。

### 真机验证

- LSPosed 注入成功；
- 主进程及多个辅助进程完成 Hook 安装；
- 成功记录真实 MTOP 请求、响应头和响应正文；
- 设置开关、刷新、清空和状态显示验证通过。
